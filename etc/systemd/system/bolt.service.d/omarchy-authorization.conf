[Service]
# Runs before Bolt enumerates hardware, including D-Bus/udev activation.
ExecStartPre=/usr/bin/bash -c 'if [[ -f /etc/omarchy/thunderbolt-authorization.enabled ]]; then exec /usr/bin/python3 /usr/share/omarchy/install/helpers/thunderbolt-setup.py guard; fi'
